<div class="row">
  <div class="col-md-3">
    <%= render 'agency_setting_sidebar' %>
  </div>
  <div class="col-md-9">
    <div class="panel panel-default">
      <div class="panel-heading">Agencies</div>
      <div class="panel-body">
        <div class="container">
          <%= link_to "Add Agency", new_agency_path, class: 'btn btn-success' %>
          <table class="table">
            <thead>
            <tr>
              <th>Name</th>
              <th>Created at</th>
            </tr>
            </thead>
            <% @agencies.each do |agency| %>
              <tr>
                <td><%= agency.name %></td>
                <td><%= agency.created_at %></td>
              </tr>
            <% end %>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>
