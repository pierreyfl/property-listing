json.extract! project, :id, :user)id, :country, :building_name, :street_address, :suburb, :state, :postcode, :display_address, :title, :description, :key_features, :prefix, :currency, :price, :created_at, :updated_at
json.url project_url(project, format: :json)
